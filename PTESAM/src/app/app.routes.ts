import { Routes } from '@angular/router';
import { MainMenuComponent } from './Views/MainMenu/main-menu/main-menu.component';

export const routes: Routes = [
    {path: '', component:MainMenuComponent}
];
